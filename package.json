{
  "name": "filz",
  "version": "0.0.1",
  "description": "Simple tool for creating own cli-s based on commander",
  "main": "index.js",
  "scripts": {
    "build": "node script/build && tsc",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cli", "framework", "commander", "nested"],
  "author": "Jakub Hajduk",
  "license": "ISC",
  "dependencies": {
    "commander": "^9.4.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.5",
    "dotenv": "^16.0.1",
    "esbuild": "^0.14.49",
    "typescript": "^4.7.4"
  }
}
